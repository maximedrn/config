# Config

Tool and agent configurations that can be copied from one computer to another.

> [!INFO]
> Last modified: August 20, 2026

## Installation

Copy and paste project to user directory:
- `$HOME` on Linux or macOS,
- `%USERPROFILE%` (`cmd.exe`) or `$env:USERPROFILE` (Powershell) on Windows.
